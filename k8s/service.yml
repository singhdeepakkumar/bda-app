apiVersion: v1
kind: Service
metadata:
  name: customer-churn-app-service
spec:
  type: NodePort
  selector:
    app: customer-churn-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
